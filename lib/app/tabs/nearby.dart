import 'package:flutter/cupertino.dart';

class NearBy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("To be implemented", style: TextStyle(fontStyle: FontStyle.italic, fontSize: 18),),
    );
  }
}